# compiler

The `cvmc` command: compiles a measured CVM image from the firmware, a
kernel and an initramfs, and recomputes the measurement of a finished one.

See [`docs/launch.md`](../docs/launch.md) for the design, the memory map, the
attestation fields and how to reproduce a published build.
