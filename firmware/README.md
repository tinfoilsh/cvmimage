# firmware

What the guest initially executes and the measured state it launches with.

See [`docs/launch.md`](../docs/launch.md) for the design, the memory
map, the attestation fields and how to reproduce a published build.
