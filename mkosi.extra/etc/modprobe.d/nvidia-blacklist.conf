# Prevent nvidia modules from auto-loading at boot
# They will be loaded by tinfoil-boot with the correct parameters
blacklist nvidia
blacklist nvidia_drm
blacklist nvidia_modeset
blacklist nvidia_uvm

# Blacklist nouveau (open-source nvidia driver) - conflicts with proprietary driver
blacklist nouveau
options nouveau modeset=0
